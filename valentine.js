if (relationship.status == "single" && valentine.interest == "you") {
    string message = "Will you be my Valentine?";
    console.log(message);
    // Optionally, if you want to add some "coding humor":
    if (heart == null) {
        heart = "❤️";
    }
    alert("You are the only bug I want to fix!");
} else {
    console.log("Not the right conditions for this request.");
}
